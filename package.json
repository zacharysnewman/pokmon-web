{
  "name": "pacman-ts",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc --noEmit && esbuild src-ts/Game.ts --bundle --outfile=dist/bundle.js",
    "dev": "esbuild src-ts/Game.ts --bundle --outfile=dist/bundle.js --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.9.3"
  }
}
